// Placeholder — a tela real (lista de posts, MOB-06 AC2) entra em T4.
// Existe aqui só para o Tabs (T1) ter as duas rotas resolvendo sem erro.
import { Text, View } from "react-native";

export default function ConteudoScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Conteúdo</Text>
    </View>
  );
}
