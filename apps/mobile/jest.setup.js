// Setup global de testes, carregado após o framework (setupFilesAfterEnv em
// jest.config.js). Vazio por enquanto — ponto único para mocks globais que
// tasks futuras (SecureStore, AsyncStorage, OneSignal) vierem a precisar em
// mais de um arquivo de teste.
